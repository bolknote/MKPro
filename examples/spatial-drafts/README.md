# Spatial Drafts

These sources are high-level MK-Pro ports for board and world games. They are
not runnable examples yet: the compiler must reject them until their rules lower
from the AST into ordinary IR.

Keep new runnable examples in `examples/`. Keep source sketches here when they
describe semantics the compiler does not lower yet.
